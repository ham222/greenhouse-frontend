name: Deploy
on:
  push:
    branches:
    - main

jobs:
  build_and_deploy:
    name: Deploy to Github Pages
    uses: ./.github/workflows/deploy.yml
    secrets:
      gh_pages_token: ${{ secrets.GH_PAGES_TOKEN }}
    